pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,jsx,tsx,cjs,mjs,cts,mts,json,jsonc,css}"
      run: bunx biome check --staged --files-ignore-unknown=true --no-errors-on-unmatched
